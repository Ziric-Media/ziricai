# Client Zero — Step 0 Reconciliation Manifest

Stabilization / reconciliation gate. **Not** Client Zero implementation.

| Ref | Commit | Meaning |
|---|---|---|
| `prod/live-2026-09-25` | `41756a4` | Known-good snapshot of what Railway runs today (P0-3C + P0-3E + appointment alignment) |
| `snapshot/main-wip-2026-09-25` | `8e63326` | Main's 158 uncommitted changes + source untracked files, preserved untouched |
| `client-zero/step0` | this branch | Production baseline + selectively reconciled main work + Step 0 hardening |
| Merge base | `d722fc5` | Common ancestor used for three-way classification |

Method: every one of the 258 paths differing between production and main was classified against `d722fc5`:
229 changed only in main, 22 only in production, 7 in both. Nothing was bulk-merged; each file below was reviewed.
`app/`, `admin/`, `marketing/` are generated by `scripts/prepare-sites.js` and were regenerated from reconciled `js/` + `css/`.

---

## 1. PROTECTED FROM PRODUCTION

Production version kept as-is (main's version rejected or already contained).

| File | Why |
|---|---|
| `services/conversationService.js` | P0-3C `assertCommunicationCompanyId`; main removes it |
| `services/tenants/conversationService.js` | P0-3C canonical-tenant-first listing; main reverts to legacy-first |
| `services/api/routeRegistry.js` | P0-3C removes `tenantOptional` on `/api/conversations`; main re-adds it |
| `api/app.js` | P0-3C routes kept; only Step 0 Sarah-chat change added (section 4) |
| `services/automation/actionExecutor.js` | Human-takeover guard (early + final), companyId requirement, Meta id check, outbound persistence. Main's inbound-skip / no-fallback-text change is **already in production** |
| `js/admin/services/conversations.js`, `js/admin/modules/conversations.js`, `js/admin/inbox-state.js` (+ generated `admin/` copies) | P0-3E: Mission Control inbox uses canonical API reply/takeover/read; main writes root Firestore `conversations` directly |
| `services/conversation/inboxMessageContract.js` | Production maps `automation` source to AI role |
| `services/conversation/canonicalCommunicationContract.js` | Production-only contract (main deletes it) |
| `services/platform/firestoreOnboardingSessionRepository.js` | Main differs only in a comment |
| `scripts/verify-corporate-p0-2.js` | Production is a superset of main (P0-2c.1 path contract) |
| `scripts/verify-corporate-p0-3{a,b,c,e}.js`, `scripts/run-corporate-p0-3ab-live-acceptance.mjs`, `p0-3ab-live-evidence.json` | P0-3 gates (main deletes them) |
| `docs/deployment/CORPORATE-P0-3*.md` | P0-3 audit docs (main deletes them) |
| `package.json` | Keeps P0-3 verify scripts (main removes them) |

## 2. BROUGHT FROM MAIN

### Backend (not live before — reviewed individually)

| File | Change | Review note |
|---|---|---|
| `services/database/firestoreClient.js` + `services/database/tenantRepository.js` | Portable query-constraint descriptors (`queryWhere/queryOrderBy/queryLimit/queryStartAfter`) | Production's constraint parser mishandles `startAfter` cursors (pagination silently returns page 1). Only `tenantRepository` uses this `query()`; self-contained |
| `scripts/verify-firestore-query-constraints.js` | Verify script for the above | Passes |
| `services/database/firestoreAdmin.js` | `ignoreUndefinedProperties: true` | Prevents Firestore write failures on `undefined` fields |
| `services/automation/triggerMatcher.js` | `MESSAGE_RECEIVED` workflows that send customer messages no longer fire without keywords (unless `matchAll`) | Stops automations auto-messaging alongside Sarah. Pairs with production's actionExecutor skip |
| `services/tools/bookTestDrive.js` | CRM sync also on duplicate bookings; passes `enriched` | Fail-safe (`.catch`); `syncTestDriveBooked` already accepts `enriched` |
| `services/tools/cancelTestDrive.js` | CRM sync on cancel (`syncTestDriveCancelled`, exists in production) | Keeps CRM aligned with appointment surfaces |
| `services/operations/commandCenterService.js` | Uses real `tenantMetrics` (already produced by `platformOperations`) instead of demo numbers; `??` instead of `\|\|` | Mission Control honesty |
| `services/tenants/aiEmployeeService.js` | `modelVersion` field on new AI employees | Additive |
| `services/platform/employeePacks.js` | **Hand-merged**: production base + main's rename "Collections AI" → "Finance AI" | Matches live catalog UI |

### Frontend (main's version is what Netlify serves today)

All backend endpoints these call were confirmed to exist in production (`/api/platform/whatsapp-numbers*`, `assign-pool-number`, `provision/operator-tenant`, `knowledge/documents`, `knowledge/upload`).
Plan-gating (`upgradeDialog`) is UI-only — no dependency on the deferred entitlement backend.

- `css/admin-dashboard.css`, `css/company-portal.css` (includes login spinner styles)
- `js/shared/loginBusy.js`, `js/admin/auth-guard.js`, `js/portal/auth-guard.js` (login spinner)
- `js/admin/api.js` — **hand-merged**: main's WhatsApp number-pool calls + production's P0-3E conversation reply/takeover/read calls
- `js/admin/demo-data.js`, `js/admin/modules/agents.js`, `js/admin/modules/companies.js` (New Company + number pool UI), `js/admin/services/agents.js`, `js/admin/services/whatsappIntegrationDisplay.js`
- `js/portal/api.js` (knowledge create/upload), `js/portal/permissions.js` (`canUploadKnowledge`, superadmin bypass), `js/portal/integrationDisplay.js`, `js/portal/upgradeDialog.js`
- `js/portal/modules/integrations.js`, `knowledge.js`, `marketplace.js`, `settings.js`, `support.js`
- `js/portal/modules/agents.js` — **hand-merged**: icon class fixes
- `js/shared/aiEmployeesCatalog.js`, `js/shared/aiEmployeesCatalog.browser.js`
- `superadmin-register.html`
- `marketing/*` (19 files: landing CSS, index + industry pages, `js/firebase*.js`, `js/shared/*`, `js/users.js`), then regenerated
- `scripts/prepare-sites.js` (CSS-fingerprint cache busting, same-origin API base, CDN importmap)
- `docs/architecture/TENANT_TAXONOMY.md`

## 3. DEFERRED (preserved on `snapshot/main-wip-2026-09-25`)

| Item | Files | Reason |
|---|---|---|
| **Pilot WhatsApp fallback — DROPPED** | `services/integrations/types/integrationConfig.js` | Decision: numbers route only through number-pool / tenant integration records. Production already does this (`findActiveWhatsAppIntegrationByPhoneNumberId`; in-memory/env mappings are dev-only). Prevents Client Zero routing through Central Motors config |
| WhatsApp prompt continuity + sales funnel | `services/ai-core/whatsappConversationPrompt.js`, `services/conversation/salesFunnelTurn.js`, `inventoryIntent.js`, `salesContext.js`, `vehicleOutboundPlan.js`, `scripts/verify-customer-identity.js`, `scripts/verify-sales-execution.js`, `verify-sales-funnel-turn.js`, `verify-inventory-intent.js`, `controlled-whatsapp-*` | **Incomplete in main**: `messageWorker.js` never passes `isNewConversation`, so alone it would mark every thread "continuing" and drop Sarah's greeting. Changes live Central Motors conversations; needs wiring + controlled test in its own step |
| Reschedule test drive | `services/tools/rescheduleTestDrive.js`, `services/conversation/appointmentScheduleAuthority.js`, `services/tools/index.js`, `services/tools/toolRunner.js`, `verify-appointment-schedule-authority.js` | New Sarah capability — out of scope for a stabilization gate |
| Portal notification handler | `services/events/portalNotificationHandler.js`, `services/events/index.js` | Hardcoded to WhatsApp (conflicts with one-brain multichannel); adds a Firestore read+write per inbound. Redesign channel-aware in Client Zero |
| Marketplace entitlements | 7 `services/platform/*Entitlement*.js`, `marketplaceInstaller.js`, `marketplaceTemplate.js`, `firestore.rules` entitlement block, `verify-portal-4c-6-marketplace-entitlement.js` | Per instruction: not brought automatically |
| `integrationHub` channel catalog | `services/integrations/integrationHub.js` | Adds pilot remap (`resolvePilotDataCompanyId`) to channels/logs. Production already sanitizes integrations. Multichannel catalog belongs to Client Zero design |
| Tenant access relaxations | `services/core/tenantContext.js` | Pilot-showcase access + membership-only (no profile) access loosen auth — contrary to Step 0 hardening |
| Pilot seed plan/billing rewrite | `services/storage/seedCentralMotorsPilot.js` | Rewrites Central Motors plan to `starter` and writes billing on every boot |
| Firebase multi-db config | `firebase.json` | Rules/index deploy tooling only; no rules deploy in Step 0 |
| Ops / probe / live-mutation scripts | ~45 `scripts/*` (e.g. `_set-railway-*`, `_link-admin-*`, `provision-*`, `real-client-1-*`, `verify-portal-4c-*`, `PORTAL-4C-*.md`, `_probe-appointments-rtb.mjs`) | One-off operational scripts; kept in the main snapshot |

## 4. NEW STEP 0 CHANGES

| File | Change |
|---|---|
| `api/app.js` | `POST /api/sarah/chat` requires a verified Firebase ID token → otherwise **401 `AUTH_REQUIRED`**. Non-superadmin callers without an explicit company use their own profile `companyId` (400 `COMPANY_REQUIRED` if none) — no silent fallback to `demo-central-motors`. P0-3C code untouched |
| `services/sarah/sarahContext.js` | Removed `role \|\| "owner"` default → `null` |
| `services/sarah/permissions.js` | `normalizeRole` no longer defaults to owner; `canUseTool` denies all tools when there is no role (superadmin unchanged) |
| `js/ziricai-landing.js`, `marketing/js/ziricai-landing.js` | Marketing widget no longer calls `/api/sarah/chat`; canned FAQ/pricing answers only |
| `services/queue/workers/messageWorker.js` | Import `saveInboundMessage` (voice-note transcript persistence). Call site already passes `companyId`. Nothing else changed |
| `scripts/verify-import-resolution.mjs` (`npm run verify:imports`) | Preflight: every relative import in `api/` + `services/` resolves and named imports are exported (catches the earlier Railway `ERR_MODULE_NOT_FOUND` class) |
| `scripts/verify-step0-sarah-lockdown.mjs` (`npm run verify:step0-sarah-lockdown`) | Boots the app, asserts anonymous / invalid-token chat → 401 `AUTH_REQUIRED`, role-less → no tools, landing has no API call |
| `package.json` | Adds the two verify scripts above |
| `docs/deployment/CLIENT-ZERO-STEP0-MANIFEST.md` | This manifest |

## 5. MISSION CONTROL INBOX — INTENTIONAL OPERATIONAL CHANGE

**This is deliberate.** Netlify `admin` currently serves main's older inbox, which writes messages and takeover state directly to the root Firestore `conversations` collection. Step 0 ships production's P0-3E inbox instead.

**Acceptance invariant:** Mission Control does not write conversation messages or takeover state directly to Firestore. It uses the canonical tenant communication APIs.

| File (source → generated deploy copy) | Role |
|---|---|
| `js/admin/services/conversations.js` → `admin/js/admin/services/conversations.js` | Data layer. `dataSource: 'api'` rows: `sendMessage` → `postConversationReply`, `setTakeoverMode` → `postConversationTakeover`, `markConversationRead` → `postConversationRead`. Simulated customer/AI messages refused for API rows |
| `js/admin/modules/conversations.js` → `admin/js/admin/modules/conversations.js` | Inbox UI; passes `companyId` + `dataSource` into the data layer |
| `js/admin/inbox-state.js` → `admin/js/admin/inbox-state.js` | Local UI overrides only (browser `localStorage`) |
| `js/admin/api.js` → `admin/js/admin/api.js` | `postConversationReply/Takeover/Read` → `POST /api/companies/:companyId/conversations/:conversationId/{reply,takeover,read}` (hand-merged with main's pool calls) |
| Backend (already live on Railway, unchanged) | Canonical routes in `api/app.js`; persistence via `services/conversationService.js` (P0-3C); takeover state via `services/conversation/takeoverSafety.js` |

Evidence (static, this commit):

| Check | Result |
|---|---|
| Generated `admin/` inbox files byte-identical to production's P0-3E versions | yes |
| Direct Firestore write calls (`addDoc/setDoc/updateDoc/updateDocument/...`) in inbox module, inbox-state, inbox-ui | 0 |
| Direct write calls in `services/conversations.js` on the **message** or **takeover** path | 0 |
| Non-API fallback for takeover / read / demo messages | browser `localStorage` only; demo messages additionally gated by `isDemoDataAllowed()` |

**Residual (disclosed, unchanged from production P0-3E):** `saveNotes()` and `saveTags()` in `js/admin/services/conversations.js` (lines 345–363) still call `updateDocument('conversations', id, …)` — a direct write of **notes/tags metadata** to the legacy root `conversations` collection, errors swallowed. Triggered only by the inbox notes box and tag editor. Not messages, not takeover, so outside the invariant above; it does not touch the canonical tenant store. Recommended follow-up: route through a tenant API or disable. **Open decision** — left as-is to keep Step 0 narrow unless you want it disabled before deploy.

**Live acceptance (required after deploy — UI loading is not sufficient):**

| # | Test | Pass condition |
|---|---|---|
| MC-1 | Reply from MC inbox to a test WhatsApp conversation | Test handset receives the message; `GET /api/companies/:companyId/conversations/:conversationId` shows it as a `human` message in the canonical tenant thread; no new doc in root `conversations` |
| MC-2 | Take over the conversation in MC | `GET` shows takeover/human mode; an inbound WhatsApp message from the test handset gets **no** Sarah reply (Railway log shows takeover skip) |
| MC-3 | Release takeover | `GET` shows AI mode; next inbound gets a Sarah reply |
| MC-4 | Open conversation (read) | Unread cleared via `/read`; persists on reload |

## 6. FIREBASE KEY CLASSIFICATION

**Expected public web configuration (not a secret):** each built `index.html` (`app/`, `admin/`, `marketing/`) contains the Firebase **web** config object with keys `apiKey, appId, authDomain, databaseId, measurementId, messagingSenderId, projectId, storageBucket`. These identify the Firebase project to the browser SDK; they are the same values already served by the live sites. Security boundary = Firebase Auth, Firestore/Storage rules, authorized domains, backend authorization (`requireTenantScope`, `requirePlatformAccess`, `requireFirebaseAuth`).

**Actual secrets / credentials — none present.** Scans run on this commit:

| Scope | Patterns | Hits |
|---|---|---|
| Deploy output `app/`, `admin/`, `marketing/` (194 html/js/json/css/toml/txt files) | OpenAI `sk-…`, private-key blocks, service-account JSON (`private_key`, `…iam.gserviceaccount.com`), Meta `EAA…`, Stripe `sk_/rk_live|test`, `whsec_`, Postgres URL with password, GitHub tokens, server env names with values (`OPENAI_API_KEY`, `WHATSAPP_TOKEN`, `FIREBASE_PRIVATE_KEY`, `RAILWAY_TOKEN`, `STRIPE_SECRET_KEY`, `PLATFORM_API_KEY`, `META_APP_SECRET`) | **0** (2 files contain `sk-demo` fake demo placeholders) |
| Full committed tree (836 files — what Railway uploads) | same | 3 hits, all **placeholders**: `.env.example` (`postgres://user:…@host`, `FIREBASE_PRIVATE_KEY="…YOUR_KEY_HERE…"`), `docs/deployment/FIRESTORE_PRODUCTION.md` (`"…\n...\n…"`) |
| Diff of all 3 pushed branches vs `origin/main` | same | 0 |
| Tracked `.env`, service-account JSON, `.pem`, `.key`, `credentials.json` on any branch | — | none |
| Worktree untracked files / local `.env` | — | 0 untracked; no `.env` in worktree (server secrets live only in Railway variables) |

## 7. KNOWN BASELINE VERIFIER FAILURES — NOT CAUSED BY STEP 0

Proof: run on `prod/live-2026-09-25` (`41756a4`) and `client-zero/step0` (`2cbb574`), same env.

| Verifier | Production snapshot | Step 0 | Failing assertion |
|---|---|---|---|
| `verify-corporate-p0-3a.js` | exit 1 @ line 37 | exit 1 @ line 37 | `schema.js` must match `/CORPORATE-P0-3\|canonical communication\|not authoritative/i` |
| `verify-corporate-p0-3c.js` | exit 1 @ line 65 | exit 1 @ line 65 | `conversationPipeline.js` must match `/NODE_ENV === "production" && !companyId/` |

Byte-identical between the two commits (git blob ids): `scripts/verify-corporate-p0-3a.js` (`08f5c1a504`), `scripts/verify-corporate-p0-3c.js` (`72bb25a0b7`), `services/database/schema.js` (`dc1a351790`), `services/integrations/conversationPipeline.js` (`4317424034`). Step 0 touched none of them, so the failures cannot be Step 0 regressions.

Classification:
- **P0-3A** — documentation-only: expects a P0-3 comment in `schema.js`. Stale verifier.
- **P0-3C** — expects a **pipeline-level** production fail-closed guard that is not in the production artifact. Tenant safety is still enforced on both sides: `webhookRouter.js` skips the pipeline for unknown `phone_number_id` (returns 200 to Meta), and `saveInboundMessage` requires `companyId` in production (`assertCommunicationCompanyId`, P0-3C). The missing pipeline guard is a defence-in-depth gap in the **baseline**, recorded for a later P0-3C follow-up — not a Step 0 change.

## Verification (local, `NODE_ENV=test`, `STORAGE_BACKEND=memory`)

| Check | Result |
|---|---|
| `node --check` on all 49 changed/added JS files | pass |
| `verify-import-resolution.mjs` (839 imports; negative test catches missing file + missing export) | pass |
| `verify-step0-sarah-lockdown.mjs` | pass |
| `verify-firestore-query-constraints.js` | pass (live Admin part skipped — no local credentials) |
| `verify-corporate-p0-1`, `p0-3b`, `p0-3e`, `verify-tenant-isolation` | pass |
| `verify-book-test-drive`, `verify-inventory-booking`, `verify-test-drive-availability` | pass |
| `verify-sales-execution`, `verify-customer-identity` | pass |
| `verify-corporate-p0-3a`, `p0-3c` | see section 7 — KNOWN BASELINE VERIFIER FAILURES |
| `prepare-sites` app/admin/marketing | built; public Firebase web config present (section 6); generated MC inbox == production P0-3E |

## Not done — awaiting authorization

- No deploy (Railway or Netlify).
- Deploy order once approved: `npm run verify:imports` gate → Railway from this worktree → Netlify `app`, `admin`, `marketing` (`--no-build`) → live acceptance:
  - anonymous `POST /api/sarah/chat` → 401 `AUTH_REQUIRED`; marketing widget answers (canned)
  - Portal Sarah works when logged in
  - WhatsApp text + voice note (transcript persisted to the tenant thread)
  - MC-1 … MC-4 (section 5)
